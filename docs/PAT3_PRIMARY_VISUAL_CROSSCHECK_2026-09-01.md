# PAT3 Primary Visual Cross-check — 2026-09-01

Source class: PRIMARY TRAINING-SLIDE SCREENSHOTS supplied directly by project owner.

These screenshots are visual evidence for the three PAT3 forms on both BUY and SELL sides. They should be read together with the direct chat rules in `DIRECT_PAT_GEOMETRY_RULES_2026-09-01.md` and the PA/PAT transcript forensics.

## BUY side

The circled BUY row shows three PAT3 variants positioned on the support line (`แนวรับ`).

Visual facts:
- all three are three-candle structures;
- candle #1 is bearish/red in the displayed examples;
- candle #2 is the differentiating candle and may appear as a small indecision/red form or a green form depending on the variant;
- candle #3 is bullish/green and is the directional completion candle in the examples;
- all displayed BUY PAT3 examples are anchored at support, reinforcing the direct rule `PA BUY only at support`.

This screenshot supports the family topology and location rule, but by itself does not prove a numeric >50% close threshold. The >50% relation comes from direct project evidence / transcript statements and must not be inferred solely from pixel geometry.

## SELL side

The circled SELL row shows three PAT3 variants positioned on the resistance line (`แนวต้าน`).

Visual facts:
- all three are three-candle structures;
- candle #1 is bullish/green in the displayed examples;
- candle #2 varies by PAT3 variant and includes a small/indecision-type middle candle in at least one displayed form;
- candle #3 is bearish/red and is the directional completion candle in the examples;
- all displayed SELL PAT3 examples are anchored at resistance, reinforcing the direct rule `PA SELL only at resistance`.

Again, the screenshot supports topology/location but not a numeric >50% denominator or tolerance by itself.

## Cross-source consequence

The current strongest combined interpretation is:

```text
PAT3 BUY:
  location = SUPPORT only
  c1 = red
  c2 = red or green, small-body / pause-style middle candle depending on variant
  c3 = green directional completion
  direct evidence says c3 closes >50% of both prior candles

PAT3 SELL:
  location = RESISTANCE only
  c1 = green
  c2 = green or red, small-body / pause-style middle candle depending on variant
  c3 = red directional completion
  direct evidence says c3 closes >50% of both prior candles
```

## Important distinction

The slide labels `PAT3 รูปแบบที่ 1`, `PAT3 รูปแบบที่ 2`, and `PAT3 รูปแบบที่ 3`. Therefore these are not separate PAT4/PAT5 families. They are three visual variants inside PAT3.

## What remains open

- exact denominator for the `>50%` close test: real body vs full High-Low range;
- exact numeric definition of `small body` for candle #2;
- exact wick requirements for each PAT3 variant;
- exact tolerance for approximately equal upper/lower wicks where applicable;
- exact distance/touch tolerance defining `at support` / `at resistance`.

Engineering status: PAT3 topology + direction + mandatory location are strongly source-constrained; numeric OHLC thresholding is still partially unresolved.
